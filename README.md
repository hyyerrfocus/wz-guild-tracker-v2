# Rebuilt WZ Guild Tracker (from deployed ZIP)

This project was reconstructed from the deployed build and your working TSX file.
To run locally:

1. npm install
2. npm run dev

To deploy, push to GitHub and connect to Vercel or Netlify (build: npm run build, publish: dist).
